package com.example.msrelations.domain;

public enum UserRole {

    ROLE_USER,   //user that has just registered in the website not using any services

    ROLE_STAFF,  //manages all info in our web services

    ROLE_ADMIN,
}
